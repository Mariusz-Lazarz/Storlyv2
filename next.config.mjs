/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "lh3.googleusercontent.com",
      "images.unsplash.com",
      "as2.ftcdn.net",
    ],
  },
};

export default nextConfig;
