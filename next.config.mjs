/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["static.nike.com", "images.unsplash.com", "as2.ftcdn.net"], // Add the domains you need here
  },
};

export default nextConfig;
