export const publicRoutes = [
  "/",
  "/test",
  "/products",
  "/not-found",
  "/api/products",
  "/api/stripe_webhook",
  "/api/autocomplete",
  "/admin",
];

export const adminRoutes = [""];

export const apiAuth = "/api/auth";
