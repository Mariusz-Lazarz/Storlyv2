export const publicRoutes = [
  "/",
  "/test",
  "/products",
  "/not-found",
  "/api/products",
  "/api/stripe_webhook",
];

export const adminRoutes = ["/admin"];

export const apiAuth = "/api/auth";
